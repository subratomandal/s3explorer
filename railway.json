{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "variables": {
      "APP_PASSWORD": {
        "description": "Admin password (min 12 chars, must contain uppercase, lowercase, number, and special char). REQUIRED for security.",
        "required": true
      },
      "SESSION_SECRET": {
        "description": "A long, random string used to sign session cookies. Use a random string generator. Change this to invalidate all active sessions.",
        "required": true
      }
    }
  }
}